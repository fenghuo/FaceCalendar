<h1>Search#group</h1>
<div id='search-box'>
  <form action='/search/group' id='search-form' method='get' target='_top'>
    <input id='search-text' name='gname' placeholder='type here' type='text'/>
    <button id='search-button' type='submit'><span>Search</span></button>
  </form>

<% if (@grs!=nil) %>
	
	<% if (@grs.kind_of?(Array) && @grs.count<1) %>
		<p>No Groups Found!</p>s
	<% else %>
		<table border='1' class='table'>
		<tr>
			<% @grs.first.each do |k,v| %>
				<th>
					<%= k.to_s %>
				</th>
			<% end %>	
		</tr>

		<% @grs.each do |row| %>
		<tr>
			<% row.each do |k,v| %>
				<td>	
					<%= v.to_s %>
				</td>
			<% end %>
		</tr>
		<% end %>
		</table>
	<% end %>
<% end %>
</div>
